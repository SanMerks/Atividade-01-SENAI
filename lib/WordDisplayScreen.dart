// WordDisplayScreen.dart
import 'package:flutter/material.dart';

class WordDisplayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Aprendizado')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Exibir palavras ou frases com traduções e opção para ouvir
          ],
        ),
      ),
    );
  }
}
