// ProgressStatsScreen.dart
import 'package:flutter/material.dart';

class ProgressStatsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Progresso e Estatísticas')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Exibir indicadores visuais de progresso e estatísticas
          ],
        ),
      ),
    );
  }
}
